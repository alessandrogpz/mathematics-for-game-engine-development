#include <gtest/gtest.h>
import std;
import geometry_triangle_meshes;

TEST(GeometryTriangleMeshes, Initialization) {
    // Just verify the placeholder module runs cleanly
    EXPECT_NO_THROW(geometry::test_geometry_triangle_meshes());
}
